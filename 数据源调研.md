# 数据源调研记录

## 1. DBLP Search API

地址：https://dblp.org/search/publ/api

用途：按论文题目、会议和年份检索公开书目元数据。当前采集器使用 JSON 返回格式、分页参数、描述性 User-Agent 和请求间隔，并把结果缓存到 SQLite。

可获得的信息主要包括题目、作者、会议、年份、DOI、DBLP 记录地址和电子版链接。DBLP Search API 不保证提供摘要和作者关键词，因此系统将这两个字段设计为可补全字段。

## 2. 本地 PDF 数据

目录：`D:\百度网盘`

导入脚本只读取已完成下载的 PDF，跳过文件名包含 `.baiduyun.` 的临时文件。标题、会议和年份优先从文件名提取，文件哈希作为本地论文的稳定标识。后续可以增加 PDF 文本抽取器，为摘要和关键词提供更准确的来源。

## 3. 备选数据源

- OpenAlex：提供论文元数据和开放获取链接，适合作为 DBLP 不可用时的补充。
- Crossref：适合通过 DOI 查询题目、作者、出版时间和容器信息。
- arXiv：适合补充预印本摘要和分类，但不能直接等同于顶会正式发表版本。
- CVF Open Access：适合 CVPR、ICCV、ECCV 的公开论文页面和 PDF 链接，需要遵守站点访问规则。

## 4. 当前选择

系统采用“本地 SQLite 优先，远程 API 按需回退”的方式。这样可以减少重复请求、保留离线可演示能力，并满足论文写入后自动重新向量化和聚类的要求。

## 5. 字段和统计来源

| 字段 | 来源 | 处理方式 |
| --- | --- | --- |
| title | DBLP 或本地 PDF 文件名 | 统一空白并保留原始标题 |
| authors | DBLP | JSON 保存 |
| venue / year | DBLP 或文件名 | 仅 CVPR、ICCV、ECCV 进入趋势统计 |
| abstract | 论文页面或 PDF（可选） | 缺失时为空 |
| keywords | PDF 文本或 TF-IDF | 统一小写，保留一元/二元词组 |
| heat | 系统计算 | 指定年份和会议中的关键词论文数量 |

## 6. 合规说明

系统只使用公开元数据和用户提供的本地文件，不绕过验证码、登录限制或访问控制。部署时不上传论文全文、个人路径和密钥。
