# 混合采集第一版

当前实现采用“批量预取 + 运行时本地优先 + 未命中联网回退”的结构。

## 文件

- `scripts/dblp_fetch.py`：批量访问 DBLP 官方 Search API，输出标准化 JSON。
- `src/storage.py`：SQLite 论文缓存，提供去重、写入、精确查询和模糊查询。
- `src/hybrid_service.py`：本地优先服务；本地无结果时调用 DBLP 并回写 SQLite。
- `app.py`：最小 HTTP API，供前端或后续 Flask/Spring 服务接入。
- `tests/test_hybrid_service.py`：本地优先、导入和 SQLite 读写测试。

## 启动本地 API

```powershell
py -3 app.py --port 8000
```

默认数据库为 `data/visionpulse.sqlite3`。只想验证本地缓存时可以关闭联网回退：

```powershell
py -3 app.py --no-online --port 8000
```

## 接口

```text
GET  /api/health
GET  /api/papers?q=vision-language&online=1
GET  /api/papers?q=Exact Title&exact=1&online=1
POST /api/papers/import
```

批量导入请求体示例：

```json
{"titles":["Paper A","Paper B"],"venue":"CVPR","year":2025}
```

`/api/papers` 会先查 SQLite。命中时返回 `source=local`；未命中且 `online=1` 时，才请求 DBLP，写入缓存后返回 `source=dblp`。

DBLP Search API 主要提供书目元数据，摘要和作者关键词不保证存在；当前版本保留 `abstract` 与 `keywords` 字段，后续由会议官网或论文页面补全。
