# 部署说明

## 课程演示部署

1. 将 `prototype.html`、`intro-research-bg.png` 和项目说明推送到 GitHub。
2. 在仓库 Settings → Pages 中选择 GitHub Actions 或发布分支。
3. 前端静态页面可以由 GitHub Pages 直接发布。
4. 本地或云服务器运行 `py -3 app.py --port 8000`，为前端提供论文和分析 API。

## 完整在线部署

完整版本需要一个能运行 Python 的后端环境。后端保存 SQLite 或托管数据库，执行采集、向量化、聚类和定时任务；前端通过 HTTPS API 访问它。GitHub Pages 不运行 Python，也不能读取本地 `D:\百度网盘`。

## 发布前检查

- 将前端 API 地址从 `127.0.0.1:8000` 替换为后端 HTTPS 地址。
- 后端只允许明确的前端来源跨域访问。
- 不提交论文 PDF、真实 SQLite、API 密钥和本地盘符路径。
- 用少量脱敏数据验证 Top 10、关键词点击和趋势动画，再发布正式数据。

## 腾讯云 Ubuntu + Docker

Ubuntu 服务器安装 Docker 后，在项目目录执行：

```bash
cp .env.example .env
nano .env
docker compose up -d --build
docker compose logs -f visionpulse
```

`.env` 至少设置 `IMPORT_TOKEN` 和 GitHub Pages 的 `FRONTEND_ORIGIN`。SQLite 保存在宿主机的 `./data` 目录，容器重启不会清空数据。腾讯云安全组和 Ubuntu 防火墙需要开放 TCP 8000；正式使用时建议再用 Nginx 配置 HTTPS。